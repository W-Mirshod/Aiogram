version: '3.8'
services:
  bot:
    build: .
    environment:
      - API_TOKEN=${API_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_URL=${OPENAI_API_URL}
    volumes:
      - .:/app
    command: python main.py
